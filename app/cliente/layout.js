import { buildNoIndexMetadata } from "@/lib/seo";

export const metadata = buildNoIndexMetadata("Ãrea cliente | Clinic Demo");

export default function ClienteLayout({ children }) {
  return children;
}

